# Story 7.2: Homebrew Cask + npm Distribution

## Goal

Enable `brew install --cask content-pipeline` on macOS and `npm install -g content-pipeline` cross-platform.

## Dependencies

- Story 7.1 (packaging)

## Files to Create/Edit

```
Caskfile                    # Homebrew cask formula
npm/package.json            # npm wrapper package
npm/bin/content-pipeline.js # CLI entry that launches Electron app
README.md                   # Installation, usage, screenshots, GIFs
```

## Acceptance Criteria

- [ ] Homebrew cask formula works for macOS
- [ ] npm global install works cross-platform
- [ ] `content-pipeline` command launches the app
- [ ] README has installation instructions + screenshots
