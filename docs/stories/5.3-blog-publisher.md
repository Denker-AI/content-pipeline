# Story 5.3: Blog Publisher (Webhook)

## Goal

Add "Publish Blog" button that POSTs blog content to a configured webhook URL (Framer CMS, Vercel, GitHub, etc.).

## Dependencies

- Story 2.2 (preview renderer)
- Story 6.2 (settings — for webhook URL)

## Files to Create/Edit

```
src/main/webhook.ts                        # Generic webhook publisher
src/main/ipc.ts                            # Update: blog publish channels
src/renderer/components/blog-publisher.tsx  # Blog publish form
```

## Implementation

- Read markdown + YAML frontmatter
- POST to configured webhook URL with: title, slug, content (HTML), metadata
- Update frontmatter with `status: published` and `published_at`

## Acceptance Criteria

- [ ] "Publish Blog" button appears for blog content
- [ ] Shows title, slug, and SEO preview
- [ ] POSTs to configured webhook URL
- [ ] Updates blog frontmatter status
- [ ] Works with any webhook endpoint (generic)
